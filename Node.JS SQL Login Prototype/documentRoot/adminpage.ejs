<!DOCTYPE html>
<html>
    <head>
        <style>
            body {
                margin: 0;
                font-family: Arial;
            }
           #topnav {
                
                overflow: hidden;
                background-color: #333;
            }

            #topnav a {
                color: #f2f2f2;
                text-align: center;
                padding: 21px 16px;
                text-decoration: none;
                font-size: 17px;

            }

            #topnav a:hover {
                background-color: #ddd;
                color: black;
            }

            #topnav a.active {
                background-color:#5eadff;
                color:black;
                font-weight:bold;
                
            }
            .floatright{
                float:right;
            }
            .floatleft{
                float:left;
            }
            #search{
                font-family: 'Arial';
                background-color: #5eadb0; /* Green */
                border: 0;
                color: white;
                height:37px;
                width:72px;
                text-align: left;
                font-weight: bold;
                display: inline;
                padding: 7px;
                font-size: 17px;
                vertical-align: middle;
            }
        </style>
    </head>
    <body>
        <div id="topnav">
            <p style="padding-left: 50px;padding-right: 50px; font-weight:bold;font-size:20px;color: white; text-shadow: 4px 4px black"" class="floatleft">Driver Hub</p>
            
            <a href="/home" class="active" style="float:left;">Home</a>
            <p style="float:right;" style="padding-right: 100px;"></p>
            <div style="margin-left:auto; margin-right:auto;overflow:hidden;display: inline;float: left;padding-left:50px;padding-top:13px;">
                
            </div>
            <div style="padding-right:100px">
                <a href="/logout" style="float:right;">Logout</a>
                <a href="/cart" style="float:right;">Cart</a>
                <a href="/mysponsor" style="float:right;">My Sponsor</a>
                <a href="/profile" style="float:right;">My Profile (<%=username%>)</a>
                <a href="/reports" style="float:right;">Reports</a>

            </div>
            
          </div>
          <br><br>
        </center>
        <center>
            <%if(userObj.User_Type == "Sponsor" || userObj.User_Type == "Admin"){%>
                <p>currently viewing as a <%=userType%></p>
              <form action="/changeUserType" method="POST">
              <select id="cars" name="usertype">
              <option value="Sponsor">Sponsor</option>
              <option value="Driver">Driver</option>
              <%if(userObj.User_Type == "Admin"){%>
                <option value="Driver">Admin</option>
                <%}%>
              </select>
              <input type="submit" value="Change View"/>
                </form>
              <%}%>
            <form action="/addPointsAdmin">
                <input type="submit" value="Add/Remove Driver Points" /><br>
            </form>
            <form action="/signupAdmin">
                <input type="submit" value="Sign up new User" /><br>
            </form>
            <form action="/manageUsers">
                <input type="submit" value="Manage all existing users" /><br>
            </form>
            <form action="/manageSponsors">
                <input type="submit" value="Manage all existing sponsor companies" /><br>
            </form>
           
        </center>
    </body>
</html>

